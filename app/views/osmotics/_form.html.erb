<%= form_for(@osmotic) do |f| %>
  <% if @osmotic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@osmotic.errors.count, "error") %> prohibited this osmotic from being saved:</h2>

      <ul>
      <% @osmotic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
